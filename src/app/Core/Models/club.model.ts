/* Este fragmento de código define una interfaz denominada `Club` en TypeScript. Una interfaz en TypeScript es
una forma de definir la estructura de un objeto. En este caso, la interfaz `Club` tiene cinco propiedades: */
export interface Club {

    id_club: number,
    nombre: string,
    codigoAcceso: string,
    localizacion: string,
    fechaCreacion: Date

}