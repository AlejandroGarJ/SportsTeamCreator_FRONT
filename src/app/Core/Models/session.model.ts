/* El fragmento de código proporcionado está escrito en TypeScript y define una interfaz llamada
`SessionUsuario`. Esta interfaz tiene tres propiedades: `dni` de tipo string, `nombre` de tipo
string y `token_session` de tipo string. */
import internal from "stream";

export interface SessionUsuario {

    dni: string,
    nombre: string,
    token_session: string

}